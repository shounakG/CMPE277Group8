<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:appcompat="http://schemas.android.com/apk/res-auto" >

    <item android:title="Refresh List"
        android:id="@+id/refreshButton"
        android:icon="@drawable/quantum_ic_refresh_white_24"
        app:showAsAction="always"
        android:actionLayout="@android:style/Theme.Holo.Light" />
    <item
        android:id="@+id/menu_search"
        android:title="@string/menu_search"
        appcompat:actionViewClass="android.support.v7.widget.SearchView"
        android:icon="@android:drawable/ic_menu_search"
        app:showAsAction="always"
        android:actionLayout="@android:style/Theme.Holo.Light" />

    <item
        android:id="@+id/filter_menu"
        android:title="Filters"
        app:showAsAction="always"
        android:icon="@drawable/filter" />

    <item android:title="Map View"
        android:id="@+id/mapView_menu"
        android:icon="@drawable/map_icon"
        app:showAsAction="always" />
    <item android:title="Logout"
        android:id="@+id/logout_menu"
        android:icon="@drawable/logout_icon"
        app:showAsAction="always" />
    <item android:id="@+id/heatMap_menu"
        android:icon="@android:drawable/ic_menu_manage"
        android:title="Heat Map"
        />
</menu>